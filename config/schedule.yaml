timezone: "Asia/Tokyo"
jobs:
  - name: "daily_ingestion_report"
    script: "/app/scripts/daily_ingestion_report.py"
    schedule: "59 15 * * *"
    timezone: "Asia/Tokyo"
    args: []
    timeout_seconds: 300
    enabled: true
    allow_concurrent: false
    max_retries: 1
    retry_delay_seconds: 60
